[defaults]
host_key_checking = False
retry_files_enabled = False
# Use built-in callback; "yaml" can resolve to community.general (not in ansible-core).
stdout_callback = default
roles_path = /opt/ansible/roles
